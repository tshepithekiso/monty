#include "monty.h"
/**
 * tt_stack - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
 */
void tt_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
